import { categories } from "../../../lib/data";
export default function CategoryDiagnostics(){return <main style={{padding:32,fontFamily:"monospace"}}><h1>Category diagnostics</h1><table><thead><tr><th align="left">Category</th><th>Answers</th><th>Ranks</th></tr></thead><tbody>{categories.map(c=>{const ranks=new Set(c.answers.map(a=>a.rank));const missing=Array.from({length:100},(_,i)=>i+1).filter(x=>!ranks.has(x));return <tr key={c.id}><td>{c.title}</td><td align="center">{c.answers.length}</td><td>{missing.length?`Missing: ${missing.join(", ")}`:"Complete"}</td></tr>})}</tbody></table></main>}
